# Local environment defaults for DropFix backend

# Optional OpenAI integration (only if enabled by code paths that use it)
OPENAI_API_KEY=

# API server settings
HOST=0.0.0.0
PORT=8000

# Behavior tuning
LOG_LEVEL=info

